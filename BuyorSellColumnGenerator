package generating;

import java.util.Random;

public class BuyorSellColumnGenerator {
	public String generateColumnValue(){
		String fd = new String();
		Random r = new Random();
		int radomint = r.nextInt();
		if (radomint > 0)
			fd = "buy";
		else
			fd = "sell";
		return fd;
	}
}
