package generating;

import java.util.Random;

public class InterestRateColumnGenerator {
	public String generateColumnValue(){
		String fd = new String();
		Random r = new Random();
		float rfr = 3 + 2 * r.nextFloat();
		rfr = (float) Math.round(rfr * 100) / 100;
		fd = String.valueOf(rfr);
		return fd;
	}

}
